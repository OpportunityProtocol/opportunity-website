import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(() => {
  return {
    section: {
        padding: '5% 12%',
        width: '100%',
      },
      
    descriptionImage: {
      width: '100%',
      height: 400,
    },


    fullElement: {
      width: '100%',
      height: '100%',
    },

  };
});

export default useStyles;
