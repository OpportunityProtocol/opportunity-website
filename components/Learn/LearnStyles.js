import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(() => {
  return {
   
    section: {
      padding: '5% 12%',
      width: '100%',
    },

    learnCard: {
        position: 'relative',
        margin: '15px 0px',
        padding: 20,
      },

      
  };
});

export default useStyles;
